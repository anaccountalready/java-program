package big.work.cn;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.awt.event.MouseEvent;
import java.awt.event.MouseListener;
import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;
import java.net.ServerSocket;
import java.net.Socket;
import java.net.UnknownHostException;

public class ClientSide {
	static BufferedReader reader=null;
	static BufferedWriter writer=null;
	static String line=null;
	static Socket c;
ClientSide() throws NumberFormatException, UnknownHostException, IOException{
 c=new Socket(Client.ip,Integer.parseInt(Client.cp));
if(c.isConnected()){	
Client.text2.append("客户端成功连入服务器"+Serverside.ss+"\n");
 reader=new BufferedReader(new InputStreamReader(c.getInputStream()));
 writer=new BufferedWriter(new OutputStreamWriter(c.getOutputStream()));

	}}
public void run() throws IOException {
	// TODO Auto-generated method stub
		new Thread(){public void run(){
			 try {
				while((line=reader.readLine())!=null){
						Client.text2.append("客户端已接受到：服务器的消息："+Server.text1.getText()+"\n");
}              
			} catch (IOException e) {
				// TODO Auto-generated catch block
				e.printStackTrace();
			}}}.start();
		new Thread(){public void run(){
			Client.o=true;
			//在发送按钮上添加监视器
			while(Client.o) Client.b.addActionListener(new ActionListener() {@Override
				public void actionPerformed(ActionEvent arg0) {
					// TODO Auto-generated method stub
					Client.text2.append("向客户端发送"+Client.text1.getText()+"\n");
					try {
						writer.write(Client.text1.getText());
						writer.flush();
						Client.o=false;
					} catch (IOException e) {
						// TODO Auto-generated catch block
						e.printStackTrace();
					}
				}
			});
		}}.start();
	 
}

public static void main(String[] args) throws NumberFormatException, UnknownHostException, IOException {
	new ClientSide().run();
}

}