# Fivechese五子棋项目分析

## 项目概述
Fivechese是一个基于Java Swing开发的五子棋游戏，支持本地双人对战和网络对战两种模式，包含棋盘绘制、棋子管理、胜负判断、网络通信和聊天功能。

---

## 主要模块实现逻辑

### 1. 游戏核心逻辑

#### 数据模型 (Model.java)
- **棋盘数据存储**：使用19x19的二维数组`int[][] data`存储棋盘状态，0表示空，1表示白棋，-1表示黑棋
- **棋子管理**：使用`LinkedList<Chess>`存储所有已放置的棋子
- **棋子放置**：`putChess()`方法实现棋子放置逻辑，检查位置合法性
- **胜负判断**：`judge()`方法实现四方向五子连珠检测（水平、垂直、两条对角线）
- **悔棋功能**：`back()`方法支持悔棋操作，默认一次悔两步（黑白各一步）

#### 棋盘视图 (ChessPanel.java)
- **棋盘绘制**：`drawPanel()`方法绘制19x19的棋盘网格
- **棋子绘制**：`drawchess()`方法根据棋子颜色绘制黑白棋子
- **用户输入处理**：鼠标点击事件处理，计算点击位置对应的棋盘坐标
- **游戏控制按钮**：提供"悔棋"、"开始游戏"、"重新开始"、"结束游戏"、"复盘"功能
- **响应式设计**：通过组件监听实现棋盘自适应窗口大小

#### 游戏控制 (Control.java)
- **模式切换**：支持本地模式和网络模式
- **颜色管理**：管理本地玩家和对方玩家的棋子颜色
- **游戏流程控制**：
  - 本地模式：轮流下棋，自动切换颜色
  - 网络模式：控制下棋权限，同步对方操作
- **网络消息处理**：处理来自网络的棋子放置、聊天、悔棋消息

### 2. 联网模块实现逻辑

#### 网络辅助类 (NetHelper.java)
- **网络通信基础**：使用Socket和ServerSocket实现TCP连接
- **服务端功能**：`beginListen()`方法在端口8900监听连接
- **客户端功能**：`connect()`方法连接到指定IP地址
- **消息格式**：
  - 棋子放置：`PutChess:row,col`
  - 聊天消息：`chat+消息内容`
  - 悔棋请求：`reback`
- **消息处理**：使用线程循环读取网络消息并解析执行

#### 网络面板 (NetPanel.java)
- **UI界面**：提供"开始监听"按钮、IP地址输入框、"连接服务器"按钮
- **连接控制**：调用Control类的网络连接方法

#### 聊天面板 (Chatpanl.java)
- **聊天功能**：实现实时聊天功能
- **消息显示**：使用JTextArea显示聊天记录
- **消息发送**：通过NetHelper发送聊天消息

---

## 安全风险分析

### 1. 数据传输安全风险
- **明文传输**：所有网络通信（棋子坐标、聊天消息）均为明文传输，容易被窃听和篡改
- **无加密**：未使用TLS/SSL等加密协议，存在中间人攻击风险

### 2. 输入验证安全风险
- **坐标未验证**：网络接收到的棋子坐标未进行边界检查，可能导致数组越界
- **聊天消息未过滤**：聊天消息未进行XSS防护或内容过滤

### 3. 异常处理安全风险
- **线程异常未处理**：网络监听线程和消息处理线程未正确处理异常，可能导致程序崩溃
- **连接断开未检测**：未实现心跳检测或连接断开处理机制，可能导致假死

### 4. 身份验证安全风险
- **无身份验证**：网络连接时没有任何身份验证机制，任何人都可以连接
- **无权限控制**：没有实现用户权限管理，所有用户权限相同

### 5. 其他安全风险
- **硬编码端口**：网络端口硬编码为8900，可能存在端口冲突或被恶意占用
- **错误信息暴露**：异常栈轨迹直接打印到控制台，可能泄露敏感信息

---

## 总结

Fivechese五子棋项目实现了基本的五子棋游戏功能和网络对战功能，但在安全性方面存在较多问题，主要集中在数据传输、输入验证和身份验证等方面。